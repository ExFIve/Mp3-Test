/**
 * Stub for optional `react-native-fs` import inside `jsmediatags` (React Native only).
 * Web builds never execute that path; Vite still needs a resolvable module.
 */
const reactNativeFsStub = {};
export default reactNativeFsStub;
